#ifndef BOOK
#define BOOK
#include "Publication.h"
#include "Sales.h"

class Book : public Publication, public Sales{
	private:
		int m_pageCount;
		int m_year;
	protected:
	public:
		Book();
		~Book();
		int PageCount();
		void PageCount(int pageCount);
		int Year();
		void Year(int year);
};

#endif