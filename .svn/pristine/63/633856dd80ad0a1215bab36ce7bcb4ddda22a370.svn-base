#ifndef BOOK
#define BOOK
#include "Publication.h"
#include <cstring>
#include <iostream>
using std::endl;
using std::cout;
using std::cin;

class Book : public Publication{
	private:
		int m_pageCount;
		int m_year;
	protected:
	public:
		Book();
		~Book();
		int PageCount();
		void PageCount(int pageCount);
		int Year();
		void Year(int year);

		void GetData();
		void PutData();
};

#endif